New comment on "<%= @post.title %>"
<%= "=" * (@post.title.length + 17) %>

By <%= @comment.user.name %>

<%= @comment.body %>

Visit onine at <%= topic_post_url(@post.topic, @post,
                                  anchor: "comment-#{@comment.id}") %>
